name: pqcrypto
description: Pure Dart post-quantum cryptography library. Starts with FIPS 203-aligned ML-KEM (Kyber) and future ML-DSA support. Flutter and Web compatible.
version: 0.2.1
homepage: https://github.com/turkananation/pqcrypto
repository: https://github.com/turkananation/pqcrypto

environment:
  sdk: ^3.10.0

# Add regular dependencies here.
dependencies:
  pointycastle: ^4.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
