name: pqcrypto_interop_common
description: Shared algorithm metadata and test helpers for pqcrypto interop tools.
publish_to: none
version: 0.4.0-dev

environment:
  sdk: ^3.10.0

dependencies:
  pqcrypto:
    path: ../..

dev_dependencies:
  lints: ^6.0.0
